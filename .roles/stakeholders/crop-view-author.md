---
name: CROP View Author
slug: crop-view-author
tier: stakeholder
primary_concern: reusable metadata and frontmatter predicates
---

# CROP View Author

## Primary concerns

- CROP should be able to replace local view/frontmatter parsing with SLICE when
  parity is proven.
- CROP still owns graph cuts, corpus health, and view policy.
- Selector errors should be actionable in `crop view` and `crop inspect`.
- Metadata predicates should compose without making CROP depend on Pebble policy.
