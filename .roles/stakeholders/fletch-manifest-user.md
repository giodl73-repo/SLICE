---
name: FLETCH Manifest User
slug: fletch-manifest-user
tier: stakeholder
primary_concern: manifest and cacheline slicing
---

# FLETCH Manifest User

## Primary concerns

- FLETCH manifests, partitions, and quivers need reusable local selectors.
- FLETCH still owns fetch/cache identity, freshness, verification, and ledgers.
- SLICE should make offline manifest inspection easier, not imply network IO.
- Query examples should work over JSON fixtures before runtime adoption.
